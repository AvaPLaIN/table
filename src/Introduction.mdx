import { Meta } from "@storybook/blocks";

<Meta title="Table" />

# Welcome to the Table Component

Our Table Component supports a variety of features to facilitate your work. It supports both server-side and client-side operations, filter and sort functions, selectable rows, column grouping, bar charts, checkboxes, row limit, and resizing.

<div className="subheading">Features</div>

<div className="link-list">
  <div className="link-item">
    {/* <img src={ServerSide} alt="server-side" /> */}
    <span>
      <strong>Server-side operations</strong>
      Supports server-side data processing
    </span>
  </div>
  <div className="link-item">
    {/* <img src={ClientSide} alt="client-side" /> */}
    <span>
      <strong>Client-side operations</strong>
      Supports client-side data processing
    </span>
  </div>
  // ... And so on for each feature ...
</div>
